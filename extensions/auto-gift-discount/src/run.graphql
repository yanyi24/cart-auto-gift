query RunInput($inCollectionIds: [ID!]!, $inTags: [String!]!) {
  cart {
    buyerIdentity {
      customer {
        id
      }
    }
    lines {
      id
      quantity
      cost {
        totalAmount {
          amount
        }
      }
      merchandise{
        ... on ProductVariant{
          id
          product {
            inCollection: inAnyCollection(ids: $inCollectionIds)
            inTags: hasAnyTag(tags: $inTags)
          }
        }
      }
    }
  }
  discountNode {
    metafield(namespace: "$app:auto-gift", key: "amount-configuration") {
      value
    }
  }
}
